n = int(input())

counter = 0 

for rows in range(1, n + 1):
    for cols in range(rows):
        counter += 1
        
        if rows != cols:
            print(counter, end=" ")
        else:
            print(counter)
        
        if counter == n:
            quit()
    print()
         
